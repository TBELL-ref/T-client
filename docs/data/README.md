# Data directory (deprecated)

Lead data is served from **Supabase**, not static JSON files.

- Snapshot: `get_published_snapshot()` RPC
- Overrides: `get_overrides_doc()` RPC
- Keywords: `get_keywords_doc()` RPC

See `private-t-client/docs/SUPABASE_MIGRATION.md`.
