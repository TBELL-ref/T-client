name: Admin Console

# Paused 2026-08-20: automatic GitHub Actions stopped.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  push-guard:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Admin data is stored in Supabase. UI saves via Supabase RPC; this workflow is a stub."
