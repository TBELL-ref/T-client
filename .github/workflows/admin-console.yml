name: Admin Console

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/admin-console.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  push-guard:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - run: echo "Admin data is stored in Supabase. UI saves via Supabase RPC; this workflow is a stub."
